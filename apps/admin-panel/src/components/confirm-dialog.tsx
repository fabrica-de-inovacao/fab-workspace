import * as Dialog from '@radix-ui/react-dialog'

export function ConfirmDialog(props: { trigger: React.ReactNode; title: string; description: string; confirmLabel: string; onConfirm: () => void; destructive?: boolean }) {
  return <Dialog.Root><Dialog.Trigger asChild>{props.trigger}</Dialog.Trigger><Dialog.Portal><Dialog.Overlay className="fixed inset-0 z-50 bg-ink/35 backdrop-blur-[2px]" /><Dialog.Content className="fixed left-1/2 top-1/2 z-50 w-[calc(100%-2rem)] max-w-md -translate-x-1/2 -translate-y-1/2 rounded-2xl border border-hairline bg-surface p-6 shadow-2xl"><Dialog.Title className="text-xl font-light tracking-tight text-ink">{props.title}</Dialog.Title><Dialog.Description className="mt-2 text-sm leading-relaxed text-ink-muted">{props.description}</Dialog.Description><div className="mt-6 flex justify-end gap-3"><Dialog.Close className="rounded-full border border-hairline-input px-4 py-2 text-sm text-ink-muted">Cancelar</Dialog.Close><Dialog.Close onClick={props.onConfirm} className={`rounded-full px-4 py-2 text-sm text-white ${props.destructive ? 'bg-error' : 'bg-primary'}`}>{props.confirmLabel}</Dialog.Close></div></Dialog.Content></Dialog.Portal></Dialog.Root>
}
